{
  "name": "f8-course",
  "version": "1.0.0",
  "description": "learn nodejs",
  "main": "src/index.js",
  "scripts": {
    "prepare": "husky install",
    "beautify": "lint-staged",
    "start": "nodemon --inspect src/index.js",
    "watch": "node-sass --watch src/resources/scss/ --output src/public/css/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.2",
    "express-handlebars": "^6.0.2"
  },
  "devDependencies": {
    "husky": "^7.0.4",
    "lint-staged": "^12.1.4",
    "morgan": "^1.10.0",
    "node-sass": "^7.0.1",
    "nodemon": "^2.0.15",
    "prettier": "^2.5.1"
  },
  "type": "module",
  "lint-staged": {
    "src/**/*.{js, json, scss}": "prettier --tab-width 4 --single-quote --trailing-comma all --write 'src/**/*.{js, json, scss}'"
  }
}
